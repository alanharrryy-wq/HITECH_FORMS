{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m pip install --upgrade pip && pip install -r requirements.txt && pip install ."
  },
  "deploy": {
    "startCommand": "bash tools/railway/start.sh"
  }
}
